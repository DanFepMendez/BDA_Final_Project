use("restaurante")

db.producto.find(
    {nombre : "BigBurger"},
    {precio: 1}).pretty()

    use("restaurante")

db.producto.find({precio: {$gt:25000},
    categoría : "Hamburguesa"})

db.producto.find({"ingredientes.helado" : "fresa",
    categoría : "Bebida"})

db.producto.find({nombre : { $gt:" "}},
    {precio: 1,nombre: 1}).pretty()
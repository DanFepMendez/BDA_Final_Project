use("restaurante")

db.producto.createIndex ({decripcion:"text"})

db.producto.find()
db.producto.find({ "descripcion": { $regex: /que combina variedad de/i } })

db.producto.find({
    $and: [
      { "descripcion": { $regex: /especial/i } }, 
      { "descripcion": { $regex: /semillas/i } }       
    ]
  })


db.producto.find({
    $and: [
      { "descripcion": { $regex: /especial/i } }, 
      { "descripcion": { $regex: /semillas/i } }       
    ]
  })